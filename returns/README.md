Saved return JSON files belong in this folder.

The Qt app's `New Return`, `Import Return`, and `Save Return As` flow is centered on this location so working return data stays separate from the master tax model.
